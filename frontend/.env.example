# Frontend production configuration
# Set BACKEND_URL to the hosted backend origin. This is used by Next.js rewrites.
BACKEND_URL=https://api.your-domain.com

# Optional: set this if you want client code to call the backend directly
NEXT_PUBLIC_API_URL=https://api.your-domain.com
